parameters:
- name: environment
  type: string
  default: Sandbox

jobs:
  - job: template_j2
      steps:
      - script: echo ${{ parameters.environment }}
  - deployment:
    displayName: "Template Deployment"
    environment: '${{ parameters.environment }}'
